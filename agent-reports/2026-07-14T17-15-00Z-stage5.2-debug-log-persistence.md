# Process Report: Stage 5.2 — Persist build-apk-debug.yml process log for post-execution analysis

- **Timestamp**: 2026-07-14T17:15:00Z
- **Task**: Stage 5.2 — Persist build-apk-debug.yml process log for post-execution analysis
- **Repo**: inscope-labs/abx-server

## 1. What was asked
The goal of Stage 5.2 is to modify the GitHub Actions debug build workflow (`.github/workflows/build-apk-debug.yml`) to capture build output logs and commit them back to the repository:
1. Change workflow-level permissions from `contents: read` to `contents: write` (noting this explicitly as a deliberate, necessary change for committing back).
2. Update the "Build debug APK" step to capture full `stdout`/`stderr` into a timestamped, run-specific log file in the `build-logs/debug/` directory.
3. Add a "Summarize warnings and errors" step immediately after "Verify APK exists" to append any matching errors, warnings, failures, or exceptions to the log file.
4. Add a "Commit build log" step at the very end to push the log file back to the repository under the name of the `github-actions[bot]`.
5. Keep all other steps in the debug workflow unchanged.
6. Explicitly ensure `build-apk-release.yml` and `build-aab-bundle.yml` remain untouched.

Drift protection checks were to be performed by fetching the live current content of `.github/workflows/build-apk-debug.yml` from GitHub before performing any edits.

## 2. Drift Protection Results
- Fetched `https://raw.githubusercontent.com/inscope-labs/abx-server/main/.github/workflows/build-apk-debug.yml` prior to editing. Compared it against the local working copy. Confirmed both matched exactly with zero drift.

## 3. What actually changed
The following files were created/modified:
- Modified `/.github/workflows/build-apk-debug.yml` with the specified changes.

### Deliberate Permission Upgrade
- Changed the workflow permissions block from `contents: read` to `contents: write`. This is a deliberate, necessary change to grant the workflow runner write-access to the repository, enabling it to push the generated log files back securely.

### Log Path Pattern
- The log files are captured under the path pattern:
  `build-logs/debug/debug-$(date -u +%Y-%m-%dT%H-%M-%SZ)-run${{ github.run_id }}.log`
  This ensures every workflow execution gets a unique, ISO-timestamped filename showing the exact build run ID.

### Workflow File Diff
```diff
--- .github/workflows/build-apk-debug.yml
+++ .github/workflows/build-apk-debug.yml
@@ -6,3 +6,3 @@
 permissions:
-  contents: read
+  contents: write
 
@@ -30,3 +30,7 @@
       - name: Build debug APK
         shell: bash
-        run: gradle --no-daemon --stacktrace :app:assembleDebug
+        run: |
+          mkdir -p build-logs/debug
+          LOGFILE="build-logs/debug/debug-$(date -u +%Y-%m-%dT%H-%M-%SZ)-run${{ github.run_id }}.log"
+          echo "LOGFILE=$LOGFILE" >> "$GITHUB_ENV"
+          gradle --no-daemon --stacktrace :app:assembleDebug 2>&1 | tee "$LOGFILE"
 
@@ -43,2 +47,11 @@
 
+      - name: Summarize warnings and errors
+        shell: bash
+        run: |
+          {
+            echo ""
+            echo "===== WARNING/ERROR SUMMARY ====="
+            grep -iE "warning:|error:|failed|exception" "$LOGFILE" || echo "None found."
+          } >> "$LOGFILE"
+
       - name: Upload debug APK
@@ -51,2 +64,11 @@
           retention-days: 14
+
+      - name: Commit build log
+        shell: bash
+        run: |
+          git config user.name "github-actions[bot]"
+          git config user.email "github-actions[bot]@users.noreply.github.com"
+          git add "$LOGFILE"
+          git commit -m "ci: add debug build log ($LOGFILE)" || echo "Nothing to commit"
+          git push
```

## 4. Workflows Untouched
- Confirmed that `.github/workflows/build-apk-release.yml` and `.github/workflows/build-aab-bundle.yml` were completely untouched, keeping the release and bundle pipelines completely isolated from logging.

## 5. Commands Ran and Their Results
- Run `compile_applet` to check project health. The project compiles successfully.
