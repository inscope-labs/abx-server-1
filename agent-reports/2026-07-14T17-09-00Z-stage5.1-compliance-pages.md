# Process Report: Stage 5.1 — Restore Play Console compliance pages

- **Timestamp**: 2026-07-14T17:09:00Z
- **Task**: Stage 5.1 — Restore Play Console compliance pages (privacy.html & delete-data.html)
- **Repo**: inscope-labs/abx-server

## 1. What was asked
The goal of Stage 5.1 is to restore Play Console compliance pages (`privacy.html` and `delete-data.html`) at the repository root, ensuring the claims are accurate for the `abx-server` repository instead of copying claims verbatim from `abx-mcp` (which implements extensive background/tunneling/filesystem behavior):
1. Create `privacy.html` and `delete-data.html` at the repository root (`/privacy.html`, `/delete-data.html`).
2. Update branding from "ABC Server" to "ABX".
3. Replace the subtitle "Auto Bridge Context Server (ABX-MCP)" with wording appropriate to `abx-server`'s role (a diagnostic / control Android application).
4. Remove or rewrite claims specific to `abx-mcp`'s real behavior that `abx-server` does not implement (audit logging, Keystore enrollment, filesystem access, MCP session gating, foreground TunnelService, SAF folder access).
5. Maintain the visual structure, CSS, dark-mode support, and the "we do not collect personal data / local-only" framing.
6. Use the email `afterdawn.service@gmail.com` and update the Last Updated date to today (`July 14, 2026`).
7. Note in the report that Play Console submission will eventually require Pages hosting or a hosting decision, but do not build it now.
8. Confirm `applicationId` and `namespace` in `app/build.gradle.kts` are both `"com.inscopelabs.abx.server"`.

Drift protection checks were to be performed by fetching the live current content of both pages from GitHub before performing any edits.

## 2. Drift Protection Results
- Fetched both `https://raw.githubusercontent.com/inscope-labs/abx-server/main/privacy.html` and `https://raw.githubusercontent.com/inscope-labs/abx-server/main/delete-data.html` from GitHub prior to creation. Both returned a `404 Not Found` response as expected, confirming no drift or overwriting risk.

## 3. Compliance Claims Refactored (Rewritten vs. Kept)

### Kept and Preserved
- **"Local-Only" and No-Data-Collection Framing:** Kept the core commitment that the application operates entirely offline and locally on the user's mobile device, with zero user credentials, tracking SDKs, cloud database storage, or remote syncs. This is still 100% accurate for `abx-server`.
- **System Theme Matching:** Preserved CSS support for automatic light and dark mode styling, utilizing a cohesive Material Design 3 palette for accessibility and legibility.
- **Contact Details:** Kept the identical support email address: `afterdawn.service@gmail.com`.

### Rewritten and Removed
- **MCP & Tunneling Features:** Completely removed references to "Auto Bridge Context Server (ABX-MCP)" and the background "TunnelService." Replaced the subtitle in both documents with **"ABX — Diagnostic & Control Android Application"** to accurately capture the scope of this repository.
- **Audit Logging & Client Sessions:** Removed all claims about audit log trails and active MCP client session gating since `abx-server` does not perform any logging of host calls or session checks.
- **Android Keystore Enrollment:** Stripped descriptions of cryptographic key generation and registration inside the Android Keystore system.
- **SAF & Folder Access:** Removed references to Storage Access Framework (SAF) folder mounting, filesystem bridge APIs, and directory mapping, as the `abx-server` application does not interact with the shared device filesystem.
- **Data Purging Steps:** Refactored the local data deletion section to instruct the user on clearing cached databases/settings purely via standard Android settings ("Storage & Cache" -> "Clear Storage") or by uninstalling the application.

## 4. Build Gradle / Compliance Configuration Checks
- Checked `app/build.gradle.kts` to verify and confirm that `applicationId` and `namespace` are set to `"com.inscopelabs.abx.server"`. They are fully compliant and were not modified.

## 5. Play Console Submission / Public Hosting Note
> [!NOTE]
> To submit these pages to Google Play Console, they must eventually be hosted publicly via a static site host (e.g. GitHub Pages or Firebase Hosting) so that Google's validation bots can read them. No hosting workflow or GitHub actions have been created in this stage in accordance with scope discipline, but a hosting decision and wiring will be required prior to production release.
